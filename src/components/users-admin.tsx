"use client";
import { useCallback,useEffect,useState } from "react";
import { Plus,UserRoundCheck } from "lucide-react";
import { ROLES,roleLabels,type Role } from "@/lib/constants";
type User={_id:string;name:string;email:string;role:Role;active:boolean};
export function UsersAdmin(){const[users,setUsers]=useState<User[]>([]);const[error,setError]=useState("");const load=useCallback(()=>fetch("/api/users").then(r=>r.json()).then(d=>setUsers(d.items||[])),[]);useEffect(()=>{load()},[load]);async function add(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);const r=await fetch("/api/users",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const d=await r.json();if(!r.ok)return setError(d.error);event.currentTarget.reset();setError("");load();}async function toggle(user:User){const r=await fetch(`/api/users/${user._id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({active:!user.active})});if(!r.ok)setError((await r.json()).error);else load();}return <><div className="page-heading"><div><p className="eyebrow">CONFIGURACIÓN</p><h1>Usuarios y permisos</h1><p>Cada persona tiene su propia cuenta y acceso según su rol.</p></div></div><div className="settings-grid"><section className="panel"><div className="panel-head"><div><h2>Nuevo usuario</h2><p>Creá una cuenta de acceso</p></div><Plus/></div><form className="settings-form" onSubmit={add}><label>Nombre<input name="name" required minLength={2}/></label><label>Correo<input name="email" type="email" required/></label><label>Contraseña inicial<input name="password" type="password" required minLength={8}/></label><label>Rol<select name="role" required>{ROLES.map(r=><option value={r} key={r}>{roleLabels[r]}</option>)}</select></label>{error&&<p className="form-error">{error}</p>}<button className="primary-btn"><UserRoundCheck size={17}/> Crear usuario</button></form></section><section className="panel"><div className="panel-head"><div><h2>Equipo</h2><p>{users.filter(x=>x.active).length} cuentas activas</p></div></div><div className="user-list">{users.map(user=><div key={user._id}><span className="avatar">{user.name.split(" ").map(x=>x[0]).join("").slice(0,2)}</span><div><b>{user.name}</b><small>{user.email} · {roleLabels[user.role]}</small></div><button className={user.active?"toggle active":"toggle"} onClick={()=>toggle(user)}><i/>{user.active?"Activo":"Inactivo"}</button></div>)}</div></section></div></>}
